<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="anandsingh.net.spreadingsmiles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login">
            <intent-filter>
                <action android:name="anandsingh.net.spreadingsmiles.login" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".options">
            <intent-filter>
                <action android:name="anandsingh.net.spreadingsmiles.options" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".volunteer" />
        <activity android:name=".thankyou2" />
        <activity android:name=".updateinfo" />
        <activity android:name=".thankyou3" />
        <activity android:name=".ngo" />
        <activity android:name=".thankyou4" />
        <activity android:name=".donor" />
        <activity android:name=".home" />
        <activity android:name=".thankyou1" />
        <activity android:name=".rest" />
        <activity android:name=".thankyou5"></activity>
    </application>

</manifest>